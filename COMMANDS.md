# Commands — Run these in Ubuntu terminal

```bash
grep -r "SelectMode\|\"normal\"\|\"sport\"\|\"loco\"\|\"ai\"\|\"arm\"" ~/ros2_ws/src/unitree_ros2/example/src/ --include="*.cpp" --include="*.hpp" | head -20
```
